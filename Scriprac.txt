-- ====================================================================
-- SCRIPT ẨN DANH NÂNG CAO - CHỐNG SPY / CHỐNG WEBHOOK DISCORD
-- ====================================================================

-- 1. Giả lập thông tin người chơi (Ẩn danh Player)
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local FakeName = "SummerUser_" .. tostring(math.random(1000, 9999))
local FakeId = 12345678 -- ID mặc định hoặc ID ngẫu nhiên

local PlayerMeta = {}
PlayerMeta.__index = function(self, key)
    if self == LocalPlayer then
        if key == "Name" or key == "DisplayName" then
            return FakeName
        elseif key == "UserId" then
            return FakeId
        end
    end
    return game.__index(self, key)
end
setmetatable(LocalPlayer, PlayerMeta)

-- 2. Chặn và bẻ gãy mọi yêu cầu gửi Webhook Discord / API bên ngoài
local HttpService = game:GetService("HttpService")
local oldPostAsync
oldPostAsync = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if self == HttpService and (method == "PostAsync" or method == "RequestAsync") then
        local url = args[1]
        if type(url) == "string" then
            local lowerUrl = url:lower()
            -- Chặn tất cả các webhook Discord và các proxy phổ biến
            if lowerUrl:find("discord.com") or lowerUrl:find("discordapp.com") or lowerUrl:find("://stripe.com") or lowerUrl:find("kaneko.dev") or lowerUrl:find("osyr.is") then
                warn("🛡️ [Ẩn Danh] Đã chặn gửi Webhook bảo vệ thông tin: " .. FakeName)
                return '{"status": "ok", "message": "spoofed"}' -- Trả về dữ liệu giả để script không bị lỗi dòng lệnh
            end
        end
    end
    return oldPostAsync(self, ...)
end)

-- 3. Xóa dấu vết phần cứng / IP trong Executor
if sethwnd then sethwnd(nil) end
if setclipboard then setclipboard("Hidden") end

print
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/jensonhirst/Orion/main/source')))()
local Window = OrionLib:MakeWindow({
    Name = "ngừng update vô thời hạn",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "KhangCaoConfig"
})

local Tab = Window:MakeTab({
    Name = "Summer Choose Scrip",
    Icon = "rbxassetid://4483362458",
    PremiumOnly = false
})

Tab:AddLabel("Script đang update, vui lòng chờ...")


Tab:AddLabel("có thể dùng script có icon 🔘")

Tab:AddButton({
	Name = "Dungeon Atom🔘",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/caiausjoncit-design/Duyvel/refs/heads/main/obfuscated_script-1782641048495.lua.txt"))()
	end
})
Tab:AddButton({
	Name = "Crimson Summer🔘⌛",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/caiausjoncit-design/Jiji/refs/heads/main/Hehe"))()
	end
})
Tab:AddButton({
	Name = "Droid🔘",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/caiausjoncit-design/N-bba/refs/heads/main/Hehe%20(1).txt"))()
	end
})
local Section = Tab:AddSection({
    Name = "Chưa hoàn thành⌛"
})
Tab:AddButton({
	Name = "Dungeon droid 🔨⌛",
	Callback = function()
	end
})
Tab:AddButton({
	Name = "Dungeon Hideout 🔨⌛",
	Callback = function()
	end
})
Tab:AddButton({
	Name = "Dungeon Mecha ⌛",
	Callback = function()
	end
})
Tab:AddButton({
	Name = "Trading plaza ⌛",
	Callback = function()
	end
})
Tab:AddButton({
	Name = "Main world ⌛",
	Callback = function()
	end
})
Tab:AddButton({
	Name = "Coming soon⌛",
	Callback = function()
	end
})
local Section = Tab:AddSection({
    Name = "⌛ là cần đợi 🔘 là Script hoạt động ổn định 🎁 Là event Coming soon "
})
local Section = Tab:AddSection({
    Name = "❌ là không ổn định Bị xoá"
})
local Section = Tab:AddSection({
    Name = "🔨 đang fix có thể sử dụng nhưng bị lỗi gì chịu"
})
